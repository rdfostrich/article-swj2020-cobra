FUNCTION queryDmCase3(store, start, end)
  snapshotVersion = store.getSnapshotBetween(start, end)
  reverseStream = store.getDeltaStream(start, snapshotVersion)
  forwardStream = store.getDeltaStream(snapshotVersion, end)
  return sortMerge(reverseStream, forwardStream)
